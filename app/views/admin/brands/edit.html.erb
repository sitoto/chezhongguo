<h1>编辑品牌</h1>

<%= render 'form' %>

<% @nav_selected_item = 'brand' %>

<% content_for :site_nav do %>
  <%= link_to '后台', admin_root_path, :class => 'first' %>
  <span class="sep">&gt;</span>
  <%= link_to '品牌', admin_brands_path %>
  <span class="sep">&gt;</span>
  <label><%= @brand.name %></label>
<% end %>
