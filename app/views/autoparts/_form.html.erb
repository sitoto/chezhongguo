<%= form_for(@autopart) do |f| %>
  <% if @autopart.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@autopart.errors.count, "error") %> prohibited this autopart from being saved:</h2>

      <ul>
      <% @autopart.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :price %><br />
    <%= f.text_field :price %>
  </div>
  <div class="field">
    <%= f.label :image %><br />
    <%= f.text_field :image %>
  </div>
  <div class="field">
    <%= f.label :description %><br />
    <%= f.text_field :description %>
  </div>
  <div class="field">
    <%= f.label :remote_url %><br />
    <%= f.text_field :remote_url %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
