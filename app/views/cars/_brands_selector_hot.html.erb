<div class="brand_selector_list">
  <div class="title_bar"><h3 class="title" style="color:Red;"><%= title %></h3></div>
  <div class="list">
    <ul>
      <% unless items.blank? %>
      <% items.each do |brand| %>
        <li>
        <%= link_to brand.name, brand, :class => 'name' %>
        </li>
      <% end %>  
      <% end %>  
    </ul>
    <div class="clear_both"></div>
  </div>
</div>
